services:
  backend:
    environment:
      AGENT_MODE: http
      ALLOW_FIXTURES: "false"
      PYTHON_BASE_URL: "${PYTHON_BASE_URL:-http://host.docker.internal:8000}"
      PYTHON_INTERNAL_TOKEN: "${PYTHON_INTERNAL_TOKEN:-}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
